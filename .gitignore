node_modules
.next
out
build
*.local
.DS_Store
coverage
playwright-report
test-results
.vercel
dist

# local env (Supabase keys)
.env.local
.env*.local

# TypeScript incremental build cache — a build artifact, and it conflicts on every merge
tsconfig.tsbuildinfo

# Supabase CLI cache
supabase/.temp/
